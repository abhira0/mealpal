-- Demo data seed moved out of the migration chain to drizzle/demo_seed.sql
-- (applied via `npm run db:seed` after migrations, so it always matches the
-- final schema). No-op kept for migration history.
SELECT 1 WHERE 0;
